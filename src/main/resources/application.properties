# Server Port
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://mysql-324faa35-thanhhosy0606-b328.i.aivencloud.com:16658/defaultdb?useSSL=true&requireSSL=true
spring.datasource.username=avnadmin
spring.datasource.password=${DB_PASSWORD}
# JPA / Hibernate cấu hình cơ bản (tùy chọn theo project của bạn)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# JPA & Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Encoding
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true

# Bank & VietQR Configuration (Napas247)
bank.name=BIDV
bank.code=BIDV
bank.bin=970418
bank.account-number=0974932089
bank.account-name=NHÃ NAM BOOK STORE

# Cấu hình Webhook Tự Động Thanh Toán (Dùng cho SePay / Casso / PayOS)
# 1. URL Webhook trên SePay: https://book-store-6q2m.onrender.com/api/webhook/payment
# 2. Phương thức: POST
# 3. Kiểu xác thực: SePay đang để 'Không xác thực' nên để là 'none'
payment.webhook.api-key=none



